---

# `/.env.example`
```text
# Database (Postgres + PostGIS)
DATABASE_URL=postgresql://postgres:password@db:5432/terror

# GTD file path inside the backend container
GTD_PATH=/data/gtd.csv

# Mapbox token for frontend
MAPBOX_TOKEN=pk.YOUR_MAPBOX_TOKEN_HERE

# Use OSM raster tiles instead of Mapbox vector tiles
USE_OSM=false

# Default model horizon days
DEFAULT_HORIZON_DAYS=90